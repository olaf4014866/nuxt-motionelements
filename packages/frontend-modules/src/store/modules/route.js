const state = {
};

const getters = {
  allQueryString: state => state.query,
};

const actions = {
};

const mutations = {
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
